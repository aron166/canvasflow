# Fallback only — a key supplied from the Settings panel takes precedence.
ANTHROPIC_API_KEY=

# Comma-separated list of origins allowed to call this backend.
CANVASFLOW_ALLOWED_ORIGINS=http://localhost:3000
